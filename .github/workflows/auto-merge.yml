# This workflow automatically merges pull requests under certain conditions.
#
# NOTE: This file is automatically synchronized from Mu DevOps. Update the original file there
#       instead of the file in this repo.
#
# NOTE: This file uses a reusable workflow. Do not make changes to the file that should be made
#       in the common/reusable workflow.
#
# - Mu DevOps Repo: https://github.com/microsoft/mu_devops
# - File Sync Settings: https://github.com/microsoft/mu_devops/blob/main/.sync/Files.yml
#
# Copyright (c) Microsoft Corporation.
# SPDX-License-Identifier: BSD-2-Clause-Patent
#

name: Auto Merge Pull Request

on:
  check_suite:
    types:
      - completed
  status: {}

jobs:
  merge_check:
    if: |
      (github.event_name == 'status' && (github.actor == 'dependabot[bot]' || github.actor == 'uefibot')) ||
      (github.event_name == 'check_suite' && github.event.check_suite.pull_requests != '[]' &&
        (github.event.check_suite.pull_requests[0].pull_request.user.login == 'dependabot[bot]' ||
         github.event.check_suite.pull_requests[0].pull_request.user.login == 'uefibot'))
    uses: makubacki/mu_devops/.github/workflows/AutoMerger.yml@test_auto_merge_adjustments
    secrets: inherit
