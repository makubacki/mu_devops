# This workflow syncs files and directories from Mu DevOps to other
# Project Mu repositories.
#
# Copyright (c) Microsoft Corporation.
# SPDX-License-Identifier: BSD-2-Clause-Patent
#
# For more information, see:
# https://github.com/BetaHuhn/repo-file-sync-action

name: Sync Mu DevOps Files to Mu Repos

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/FileSyncer.yml'
      - '.sync/**'
  workflow_dispatch:

jobs:
  sync:
    name: Repo File Sync
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Run GitHub File Sync
        uses: BetaHuhn/repo-file-sync-action@v1
        with:
          COMMIT_BODY: "Signed-off-by: Michael Kubacki <michael.kubacki@microsoft.com>"
          COMMIT_EACH_FILE: true
          COMMIT_PREFIX: "Repo File Sync:"
          CONFIG_PATH: .sync/Files.yml
          DRY_RUN: false
          FORK: false
          GH_PAT: ${{ secrets.MAKUBACKI_REPO_FILE_SYNC }}
          GIT_EMAIL: michael.kubacki@microsoft.com
          GIT_USERNAME: makubacki
          OVERWRITE_EXISTING_PR: true
          PR_LABELS: type:file-sync
          SKIP_PR: false
          # TEAM_REVIEWERS: project-mu-dependency-reviewers

